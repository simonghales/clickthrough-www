// @flow
import {css} from 'emotion';
import {HEADER_HEIGHT} from '../../components/SiteHeader/styles';

const mainClass = css`
    margin-top: ${HEADER_HEIGHT}px;
`;

export default {
  mainClass,
}
